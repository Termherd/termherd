+++
id = "F-mcp-ide-bridge"
type = "feature"
area = ["mcp"]
status = "todo"
target = ["Could"]
+++

A live MCP/IDE bridge to Claude — termherd as the client, not the server.

Live MCP/IDE bridge to Claude (moved from Unsure, PRD rev. 6); decoupled from
the still-Unsure diff panel
