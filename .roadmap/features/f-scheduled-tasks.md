+++
id = "F-scheduled-tasks"
type = "feature"
area = ["sessions"]
status = "todo"
target = ["Could"]
+++

Launch a session on a schedule rather than on a click.

Never scoped beyond the name. Adjacent to
[F-mcp-agent-loop](#f-mcp-agent-loop), which drives a session without a human;
this one would decide *when*.
