+++
id = "F-activity-stats"
type = "feature"
area = ["sessions"]
status = "todo"
target = ["Could"]
+++

Aggregate what the sessions have been doing — counts, durations, activity.

Never scoped beyond the name: neither the questions it should answer nor
where it would be shown have been decided.
