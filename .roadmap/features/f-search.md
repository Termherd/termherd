+++
id = "F-search"
type = "feature"
area = ["sidebar", "sessions"]
status = "done"
target = ["Must"]
+++

In-memory search over session digests, with a titles-only toggle.

In-memory search over digests (was `F-fts-search`; the SQLite FTS5 version
moved to Should as `F-store-cache`, PRD rev. 4) — case-insensitive, titles-only
toggle (FR3)
