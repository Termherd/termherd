+++
id = "F-foundations"
type = "feature"
area = ["packaging"]
status = "done"
target = ["Must"]
+++

Workspace, core, CI, single-instance and tracing — the project's floor.

Workspace, core, CI, single-instance, tracing
